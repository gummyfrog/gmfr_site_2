/// <reference types="node" />
import { BmpDecoder } from 'bmp-js';
export interface Color {
    red: number;
    green: number;
    blue: number;
}
interface Position {
    x: number;
    y: number;
}
interface Size {
    width: number;
    height: number;
}
export declare const drawRect: (imageDstBuffer: BmpDecoder, position: Position, size: Size, color: Color) => void;
export declare const createVisualizerFrame: (backgroundImageBuffer: BmpDecoder, spectrum: number[], size: Size, color: string | Color) => BmpDecoder;
export declare const convertToBmp: (filePath: string) => Promise<Buffer>;
export declare const parseImage: (buffer: Buffer) => BmpDecoder;
export declare const createImageBuffer: (image: BmpDecoder) => Buffer;
export declare const getImageColor: (image: BmpDecoder) => Color;
export declare const invertColor: (color: Color) => {
    red: number;
    green: number;
    blue: number;
};
export {};
