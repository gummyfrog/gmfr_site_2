extends layout

block header_extra
	svg.back(viewbox='0 0 80 48')
		a(href="/")
			text()
				tspan(x='0' y='26') HOME ∆

block content
	link(rel='stylesheet', href='/stylesheets/screenshots.css')

	div.content
		div.mediacontainer
			img.media(src="/images/chessy.png")
			div.mediatext
				h2 Duel Mail.

				p This is where a picture of the thing would go, maybe a short description. More negative space. Screenshots won't be the tallest things in the world, more like wide headers. I hate the placeholder picture I'm using.


	svg
		defs
			lineargradient#bg-gradient
				stop(offset='0%' stop-color='#E0A800')
				stop(offset='40%' stop-color='#F5B800')
				stop(offset='90%' stop-color='#E0A800')
